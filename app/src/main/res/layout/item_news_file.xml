<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_marginStart="@dimen/marginHorizontal"
    android:layout_marginEnd="@dimen/marginHorizontal"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/news_file_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/marginHorizontal"
        android:drawableStart="@drawable/ic_file"
        android:drawablePadding="8dp"
        android:gravity="center_vertical"
        android:singleLine="true"
        android:textColor="@color/blackTextColor"
        android:textSize="14sp"
        app:layout_constraintEnd_toStartOf="@id/news_file_download"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Название файла.тип" />

    <ImageView
        android:id="@+id/news_file_download"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="32dp"
        android:paddingTop="@dimen/marginHorizontal"
        android:paddingBottom="@dimen/marginHorizontal"
        android:src="@drawable/ic_save"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginTop="8dp"
        android:background="@color/dividerColor"
        app:layout_constraintTop_toBottomOf="@id/news_file_title" />


</androidx.constraintlayout.widget.ConstraintLayout>